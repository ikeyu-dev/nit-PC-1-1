export async function judge(question: string, x: number, y: number, z: number) {
    switch (question) {
        case "smile":
            return;
        case "surprised":
            return;
    }
}
